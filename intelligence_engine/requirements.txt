# ============================================================
# intelligence_engine — Python dependency manifest
# ============================================================
# Install with:
#   pip install -r requirements.txt
#
# Python version: 3.11+
# ============================================================

# --- Core LLM / LangChain stack ------------------------------------
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langgraph>=0.2.0,<0.3.0

# --- Retrieval tools -----------------------------------------------
tavily-python>=0.3.0          # Tavily web-search SDK
qdrant-client>=1.11.0         # Qdrant async client + FastEmbed support

# --- Configuration -------------------------------------------------
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# --- HTTP / async --------------------------------------------------
aiohttp>=3.9.0

# --- Evaluation (CI/CD) -------------------------------------------
deepeval>=1.4.0               # LLM evaluation framework (uses OPENAI_API_KEY for judge)
pytest>=8.0.0
pytest-asyncio>=0.23.0

# --- Observability (optional — uncomment to enable) ---------------
# langfuse>=2.0.0
# opentelemetry-sdk>=1.24.0
